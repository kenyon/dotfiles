# On my own machines this file is not necessary because the settings
# are in the systemwide /etc/ssh/ssh_config which is managed by
# puppet. This file exists for machines that I use which are not
# managed by puppet.

ForwardAgent yes
ForwardX11 yes
ForwardX11Trusted yes
VerifyHostKeyDNS yes
VisualHostKey yes

# For work network, to work around connection-killing firewalls.
ServerAliveInterval 300
